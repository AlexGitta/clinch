# Clinch Landing Page Scaffold

A React + TypeScript + Tailwind landing page starter focused on Clinch's combat-sports booking product narrative.

## Run

1. Install dependencies:

```bash
bun install
```

2. Start dev server:

```bash
bun run dev
```

3. Build production bundle:

```bash
bun run build
```
